{
  "version": 2,
  "installCommand": "npm install --force",
  "buildCommand": "cd client && npx vite build --outDir ../output",
  "outputDirectory": "output",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
