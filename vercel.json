{
  "version": 2,
  "installCommand": "npm install --force",
  "buildCommand": "npm run build && cp -r dist/public vercel_output",
  "outputDirectory": "vercel_output",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
